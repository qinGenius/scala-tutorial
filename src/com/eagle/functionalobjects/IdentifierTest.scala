package com.eagle.functionalobjects

object IdentifierTest {

  def main(args: Array[String]): Unit = {
    //如果字符后面直接加underscore 那么这个变量必须和冒号之间有个空格，否则会编译报错
    val name_ :Int=1

    //A literal identifier is an arbitrary string enclosed in back ticks (` . . . `). Some examples of literal identifiers are:
     // `x` `<clinit>` `yield`
    println()

  }

}
